name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # TODO: add deployment steps (Docker build & push, cloud deploy, etc.)
      - run: echo "Deploy step — configure for your target environment"
